<% flash.each do |key, value| %>
  <div class="alert alert-dismissable alert-<%= key == 'alert' ? "danger" : "success" %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <%= value%>
  </div>
<% end %>